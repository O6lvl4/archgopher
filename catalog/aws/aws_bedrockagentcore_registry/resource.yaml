type: aws_bedrockagentcore_registry
label: Agent Registry
category: AI
icon: agentcore
description: "Records stored and search calls. The monthly free 5,000 records and 1,000,000 searches are given once for the account."
kinds: [search]
sla: aws.agentcore
assumptions:
  - { key: records, label: Registry records, type: number, unit: records }
readings:
  - cost: { name: Records stored, quantity: records, unit: record-month, price: aws.agentcore.registry.records }
  - cost: { name: Search API, quantity: total.monthly, unit: request, price: aws.agentcore.registry.search }
  - rate: { name: Search rate, unit: requests/second, quota: aws.agentcore.registry.search_rps, peak: total.peak }
terraform:
  ignoreRefs: [authorizer_configuration]
iam:
  search: ["bedrock-agentcore:SearchRegistryRecords"]
